spring:
  datasource:
    url: jdbc:h2:tcp://localhost:9090/~/fiap-card;DB_CLOSE_ON_EXIT=FALSE
    username: fiap
    password: fiap
  batch:
    jdbc:
      initialize-schema: always
      schema: classpath:schema-h2.sql

file:
  resource: lista_alunos.txt
  resource-cartao: lista_cartoes2.txt

